---
title: "Model run example"
format: html
editor: visual
---

```{r}
library(yaml)
library(tidyverse)
params <- read_yaml('../params/params.yaml')
devtools::load_all()

```
Create a starting population

```{r}

dd <- generate_pop(params)
dd

```

```{r}

dd <- sim_day(dd,'monday',params)

count(dd,gp_app)
```
